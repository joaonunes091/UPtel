@model UPtel.Models.TipoClientes

@{
    ViewData["Title"] = "Detalhes do tipo de cliente";
}
<section id="secondhero" class="d-flex flex-column justify-content-center">
    <div class="container">
        <div class="row justify-content-center text-left">
            <div class="col-xl-8 text-white">
                <h2>Detalhes do tipo de cliente</h2>

                <div>
                    <br/>
                    @* <div class="row">*@
                    @*<div class="col-md-4">*@
                    <dl class="row">
                        <dt class="col-sm-2 text-white">
                            @Html.DisplayNameFor(model => model.Designacao)
                        </dt>
                        <dd class="col-sm-10 text-white">
                            @Html.DisplayFor(model => model.Designacao)
                        </dd>
                    </dl>
                    @*  </div>
        </div>*@
                </div>
                <div>
                    <a class="btn btn-costum" asp-action="Edit" asp-route-id="@Model.TipoClienteId">Editar</a> |
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@Model.TipoClienteId">Eliminar</a> |
                    <a class="btn btn-info" asp-action="Index">Voltar</a>
                </div>
            </div>
        </div>
    </div>
</section>
